# 🧪 دليل النشر التجريبي (Staging) — بدون أي مخاطرة على الموقع الحالي

هذا الدليل يشرح كيف تجرّب النسخة الجديدة على **سيرفر تجريبي** (IP مباشر أو دومين فرعي)
بينما يبقى موقعك القديم `trbhh.com` **شغّالاً كما هو تماماً**.

> ✅ **مبدأ الأمان:** لا نلمس إطلاقاً دومين `trbhh.com` الرئيسي ولا سيرفره ولا قاعدته.
> النسخة التجريبية مستقلة بالكامل، وتقرأ الصور من الموقع الأصلي بشكل **قراءة فقط** أثناء التجربة.

---

## المتطلبات
- خادم VPS من Hostinger (Ubuntu 24.04) — **غير** الخادم الذي عليه الموقع القديم.
- Docker و Docker Compose مثبّتان (`curl -fsSL https://get.docker.com | sh`).
- ملف قاعدة البيانات `database/trbhh.sql.gz` (موجود في المشروع).

---

## الخيار (أ) — التجربة عبر IP مباشرة (الأسرع، بدون دومين)

مناسب لتجربة سريعة دون أي إعداد DNS.

```bash
# 1) على الخادم التجريبي
git clone <REPO_URL> trbhh && cd trbhh
git checkout claude/hostinger-vps-project-amw8vb

# 2) الإعدادات
cp .env.example .env
nano .env
#   - DB_PASSWORD / DB_ROOT_PASSWORD : كلمات مرور قوية
#   - AUTH_SECRET : ألصق ناتج  openssl rand -hex 32
#   - LEGACY_MEDIA_BASE=https://trbhh.com   (لجلب الصور القديمة أثناء التجربة)

# 3) التشغيل (يستورد البيانات تلقائياً عند أول مرة)
docker compose up -d --build
```

افتح المتصفح على: **`http://<عنوان-الخادم-IP>`**

> إذا أردت منفذاً مخصّصاً بدل 80، عدّل منفذ خدمة `nginx` في `docker-compose.yml` مثلاً `"8080:80"`
> ثم افتح `http://<IP>:8080`.

---

## الخيار (ب) — التجربة على دومين فرعي (مثل `new.trbhh.com`)

يبقى `trbhh.com` و `www.trbhh.com` على الموقع القديم دون أي تغيير — نضيف **سجلّاً واحداً فقط** للفرع الجديد.

### 1) إضافة سجل DNS للفرع الفرعي فقط
من لوحة تحكم الدومين، أضِف سجلّاً واحداً:

| Type | Name  | Value (Points to)      |
|------|-------|------------------------|
| A    | `new` | `IP الخادم التجريبي`   |

> ⚠️ **لا تعدّل** سجلّات `@` أو `www` — هذه تخصّ الموقع القديم وتبقى كما هي.

### 2) تهيئة الدومين الفرعي في الإعداد
عدّل `docker/nginx.conf` واستبدل `trbhh.com` بـ `new.trbhh.com` في سطر `server_name`.

### 3) التشغيل + شهادة SSL للفرع الفرعي فقط
```bash
cp .env.example .env && nano .env      # كما في الخيار أ
docker compose up -d --build
bash scripts/init-ssl.sh new.trbhh.com admin@trbhh.com
# ثم فعّل بلوك 443 في docker/nginx.conf وأعد تشغيل nginx:
docker compose restart nginx
```

افتح: **`https://new.trbhh.com`**

---

## بعد التجربة — الانتقال للإنتاج (عندما تقرّر أنت فقط)

عندما تطمئن للنسخة الجديدة، وبقرار منك أنت:
1. خذ نسخة احتياطية حديثة من قاعدة الموقع القديم واستوردها في الخادم الجديد
   (`bash scripts/import-db.sh <ملف-النسخة>.sql.gz`).
2. انسخ مجلد الصور/التخزين من الموقع القديم إلى `./storage` في الخادم الجديد
   (بعدها تُخدَم الصور محلياً وتلغي الحاجة إلى `LEGACY_MEDIA_BASE`).
3. حدّث `server_name` إلى `trbhh.com www.trbhh.com` وأصدر شهادة SSL لهما.
4. **في وقت تختاره**، غيّر سجلّات DNS الرئيسية (`@` و `www`) لتشير إلى الخادم الجديد.

> حتى هذه اللحظة يبقى الموقع القديم يعمل. التحويل النهائي بيدك وحدك.

---

## نقاط أمان مهمة
- 🔒 النسخة التجريبية **لا تكتب** أي شيء في الموقع القديم ولا قاعدته.
- 🖼️ `LEGACY_MEDIA_BASE` يجلب الصور القديمة **قراءة فقط** (لا يعدّل شيئاً).
- 💳 المنصة ليست وسيطاً مالياً — كل تعامل ودفع خارج المنصة (ظاهر في كل الصفحات).
- 🔑 غيّر `AUTH_SECRET` وكلمات مرور قاعدة البيانات قبل أي نشر.
- 🚫 لا تشارك رابط النسخة التجريبية للعامة قبل اكتمال مراجعتك.

## أوامر مفيدة
```bash
docker compose ps                 # حالة الحاويات
docker compose logs -f app        # سجلّات التطبيق
docker compose down               # إيقاف (تبقى البيانات)
bash scripts/backup.sh            # نسخة احتياطية للقاعدة
```
