# سجل مراقب الأخطاء التلقائي

يفحص هذا النظام سجل `error_log` كل ساعة، ويقارنه بـ `ops/error-watch-state.json`.
عند اكتشاف نوع خطأ جديد لم يسبق تشخيصه: يُشخَّص السبب الجذري، يُكتب له إصلاح على
فرع منفصل، ويُفتح طلب سحب (Pull Request) للمراجعة — **لا يُدمَج ولا يُنشر تلقائياً
على trbhh.com إطلاقاً**. نقطة التراجع دائماً متاحة: إغلاق الـ PR بدون دمج يلغي
التغيير بالكامل بلا أي أثر على الموقع الحي.

---

## 2026-07-23 — تفعيل النظام

بدء المراقبة الساعية. الحالة الأولية: 0 أخطاء في السجل (نُظِّف بالكامل هذا اليوم
بعد إصلاح ونشر الأنواع الثلاثة المعروفة: `ad_actions`, `chunk_load`, `remove_child`
— انظر `error-watch-state.json`).
