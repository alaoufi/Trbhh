# دليل المطوّر — منصة تربح (Trbhh)

منصة إعلانات مبوّبة وأعمال تجارية B2B، عربية RTL، مبنية بـ **Next.js 15 (App Router)**
و **TypeScript** و **Prisma** على **MySQL 8**، مع **Redis** للكاش، ومنشورة على
**Hostinger VPS** عبر **Docker Compose**. الموقع المباشر: https://trbhh.com

> المنصة **ليست وسيطاً مالياً** — دورها عرض الإعلانات وربط الأطراف فقط، وكل تعامل أو
> دفع يتم خارج المنصة مباشرة بين الطرفين.

---

## 1) محتويات هذه النسخة الاحتياطية

| المجلد/الملف | المحتوى |
|---|---|
| `src/` | كل كود التطبيق (صفحات، مكوّنات، مكتبات، Server Actions) |
| `prisma/schema.prisma` | مخطط قاعدة البيانات (مستخرج من القاعدة الأصلية) |
| `database/trbhh.sql.gz` | نسخة قاعدة البيانات (mysqldump مضغوط) |
| `scripts/` | سكربتات النشر والنسخ الاحتياطي والاستيراد و SSL |
| `docker-compose.yml` · `Dockerfile` | حاويات التشغيل |
| `.env.example` | نموذج متغيّرات البيئة (انسخه إلى `.env` واملأ القيم) |
| `apps/android-twa/` | غلاف تطبيق أندرويد (TWA) |
| `دليل-المطور.md` | هذا الملف |

> **مهم:** ملف `.env` الحقيقي (كلمات المرور والأسرار) **غير مضمّن** في هذه النسخة
> لأسباب أمنية. أنشئه من `.env.example`. وكلمة مرور توقيع تطبيق أندرويد **ليست في
> المستودع** ويحتفظ بها المالك.

---

## 2) التقنيات

| الطبقة | التقنية |
|---|---|
| الواجهة | Next.js 15 (App Router) · React 19 · TypeScript · TailwindCSS |
| الخادم | Server Components + Server Actions |
| قاعدة البيانات | MySQL 8 · Prisma ORM |
| الكاش/الجلسات | Redis (ioredis) · جلسات JWT موقّعة (jose) |
| الصور | next/image + مسار وسائط موحّد `/media` |
| النشر | Docker · Nginx · Let's Encrypt |
| حزمة الحزم | pnpm |

---

## 3) التشغيل محلياً للتطوير

```bash
# 1) المتطلبات: Node.js 20+، pnpm، Docker (اختياري لقاعدة بيانات محلية)
pnpm install

# 2) البيئة
cp .env.example .env
#   عدّل DATABASE_URL و AUTH_SECRET (ولّده: openssl rand -hex 32)

# 3) قاعدة بيانات محلية للتجربة (اختياري)
docker compose --profile localdb up -d db redis
bash scripts/import-db.sh database/trbhh.sql.gz
npx prisma generate

# 4) التشغيل
pnpm dev            # http://localhost:3000
```

### بوابات الجودة (شغّلها قبل أي دفع)
```bash
npx tsc --noEmit    # فحص الأنواع
pnpm lint           # ESLint
pnpm test           # Vitest (35 اختبار)
pnpm build          # بناء الإنتاج
```

---

## 4) بنية المشروع

```
src/
  app/                 صفحات App Router + Server Actions (actions.ts بكل مجلد)
    (public)           الرئيسية، الأقسام، البحث، تفاصيل الإعلان، المتاجر…
    account/           لوحة العضو: الإعلانات، المحفظة، الملف، الشركة
    admin/             لوحة الإدارة (محمية بالصلاحيات) + admin/guide دليلها
    ads/ classified/   الإعلانات والإعلانات المبوّبة
    api/  media/       مسارات API وخدمة الوسائط
  components/          مكوّنات واجهة قابلة لإعادة الاستخدام
  lib/                 منطق الخادم: prisma, auth, roles, wallet, settings, data…
  data/schema-sync.ts  ضبط أعمدة/جداول ناقصة تلقائياً عند الإقلاع
prisma/schema.prisma   مخطط Prisma
scripts/               نشر ونسخ احتياطي واستيراد و SSL
```

### مفاهيم أساسية
- **الصلاحيات:** `src/lib/roles.ts` — `requireAction(service, action)` و`requireAnyAdmin()`.
- **الإعدادات والنصوص:** `src/lib/settings.ts` — كل نص/مفتاح قابل للتعديل من لوحة الإدارة
  (النصوص الظاهرة، الإعدادات) عبر جدول `site_settings`. لا تكتب نصوصاً ثابتة في الكود.
- **المحفظة:** `src/lib/wallet.ts` — `charge/adjustBalance` وأسباب الحركات `TxnReason`.
- **مخطط متسامح:** `src/data/schema-sync.ts` يضيف الأعمدة الناقصة تلقائياً — عند إضافة
  عمود جديد، أضِفه هنا **و** في `prisma/schema.prisma`.
- **توافق كلمات المرور القديمة:** دخول الأعضاء الحاليين يعمل بكلمات مرورهم القديمة
  (bcrypt `$2y$`).
- **سجل الأخطاء التقنية:** أي خطأ يعترض عضواً (`error.tsx`/`classified/error.tsx`/
  `global-error.tsx`) يُسجَّل تلقائياً عبر `reportClientErrorAction` (`src/app/error-actions.ts`)
  في جدول `error_log` — راجعه من لوحة الإدارة ← «سجل الأخطاء التقنية» (`/admin/errors`)
  عند أي بلاغ عطل بدل التخمين. عمليات حسّاسة (كحفظ ملف مرفوع) يُستحسن تغليفها بـ
  `try/catch` + `logClientError()` (`src/lib/error-log.ts`) بدل تركها تنفجر لشاشة الخطأ العامة.
- **المنطقة والمدينة (السعودية فقط):** جدول `cities` = المناطق (13 منطقة رسمية)،
  وجدول `areas` = المدن/المحافظات داخل كل منطقة (`cityId` مرتبط بـ `cities`).
  `ads.city_id` = المنطقة (إلزامي)، `ads.area_id` = المدينة (اختياري). لا تستخدم
  `<select>` مباشراً على `getCities()` باسم «المدينة» — استخدم مكوّن
  `RegionCityPicker` (نماذج الإعلانات) أو `SearchAreaPicker` (صفحات التصفية/البحث)،
  وفلتر دائماً `countryId === 1` لاستبعاد أي دول غير سعودية في الجدول.
- **توثيق تاريخي للأقسام القديمة (تجاوزه تنفيذ الأقسام الجديد في § 10):** كانت الأقسام مخفية بقرار متعمّد.
  `categoriesEnabled()` في `src/lib/settings.ts` (مفتاح `cats_on`) تتحكم بذلك، وافتراضها
  **`false`**: أي فشل أو غياب لقيمة الإعداد يجب أن يبقيها مخفية، لا أن يُظهرها — لهذا كل
  استدعاء لها يجب أن يكون `.catch(() => false)` وليس `.catch(() => true)`. أي عرض لاسم
  قسم أو رابط `/categories` في أي واجهة عامة يجب أن يُحرَس بـ`catsOn &&` — البيانات
  الداخلية (`category_id` على الإعلان) تبقى محفوظة وتُسند تلقائياً لقسم «عروض أخرى»
  (`getFallbackCategoryId()` في `src/lib/data.ts`) دون أي واجهة تختار قسماً يدوياً.
  إعادة الإظهار مستقبلاً: من لوحة الإدارة ← الإعدادات ← «إظهار الأقسام في الموقع».

---

## 5) النشر على الخادم (Hostinger VPS)

المستودع على الخادم في `/root/trbhh`، وخدمة التطبيق في docker compose اسمها `app`.
فرع التطوير الحالي: `claude/hostinger-vps-project-amw8vb`.

### النشر التلقائي (الطريقة الأساسية)

كل دفع (`git push`) لفرع `claude/hostinger-vps-project-amw8vb` يُشغّل تلقائياً سير عمل
GitHub Actions **`.github/workflows/deploy.yml`**: يتّصل بالخادم عبر SSH، يسحب أحدث الكود،
ويعيد بناء وتشغيل خدمة `app` في docker compose — **بلا أي تدخل يدوي**. يمكن تشغيله يدوياً
أيضاً من تبويب **Actions** في المستودع (`Run workflow`).

**الأسرار المطلوبة** (Settings → Secrets and variables → Actions في المستودع):
| السرّ | القيمة |
|---|---|
| `VPS_HOST` | عنوان IP الخادم |
| `VPS_USER` | `root` |
| `VPS_PORT` | اختياري — افتراضياً 22 |
| `VPS_SSH_KEY` | المفتاح الخاص (نص خام كامل الأسطر، من `BEGIN` إلى `END`) — مفتاحه العام مُضاف مسبقاً في `~/.ssh/authorized_keys` على الخادم بتعليق `github-actions-deploy@trbhh` |

السجلات: تبويب **Actions** في المستودع، أو من الخادم `docker compose logs -f app`.

### النشر اليدوي (بديل احتياطي)

يعمل دائماً بغضّ النظر عن حالة النشر التلقائي:

```bash
cd /root/trbhh
git pull origin <اسم-الفرع>
docker compose up -d --build app
docker compose ps
```

- إعادة التشغيل: `docker compose restart app`

### متغيّرات البيئة المهمة (`.env`)
- `DATABASE_URL` — رابط قاعدة البيانات (رمّز الأحرف الخاصة في كلمة المرور: `@`→`%40`، `#`→`%23`).
- `AUTH_SECRET` — سر توقيع الجلسات (`openssl rand -hex 32`).
- `STORAGE_DIR` — مجلد الوسائط المرفوعة (مربوط كـ volume).
- `COOKIE_SECURE=true` خلف HTTPS فقط.
- التفاصيل الكاملة في `.env.example`.

---

## 6) قاعدة البيانات — نسخ واستعادة

### نسخة احتياطية للقاعدة الحيّة (على الخادم)
```bash
cd /root/trbhh
bash scripts/backup.sh          # ينتج backups/trbhh-<تاريخ>.sql.gz (يحفظ آخر 14)
```

### نسخة احتياطية كاملة (كود + قاعدة حيّة + وسائط + هذا الدليل) في ملف واحد
```bash
cd /root/trbhh
bash scripts/full-backup.sh     # ينتج تربح-<تاريخ>-<وقت>.zip
```

### استعادة قاعدة بيانات من ملف
```bash
bash scripts/import-db.sh <ملف.sql.gz>
```

> ملاحظة: `database/trbhh.sql.gz` المضمّن في النسخة هو لقطة بتاريخ إنشائه، وليس
> بالضرورة أحدث بيانات الإنتاج. للحصول على أحدث البيانات استخدم `scripts/backup.sh`
> أو `scripts/full-backup.sh` على الخادم.

---

## 7) تطبيق أندرويد (TWA)

- المجلد: `apps/android-twa/` — الحزمة `com.trbhh.app`، المُوقّع (alias) `trbhh`.
- **كلمة مرور المفتاح (keystore) ليست في المستودع** — يحتفظ بها المالك، وتُدخل يدوياً عند البناء.
- الإصدار يُرفع في ملفات إعداد Gradle (versionName/versionCode).

---

## 8) قواعد ثابتة للمشروع (اتبعها في أي تعديل)

1. شغّل بوابات الجودة (tsc/lint/test/build) **قبل أي دفع**.
2. رسائل الالتزام (commit) بالعربية.
3. أي ميزة جديدة يجب أن تكون **قابلة للتحكم من لوحة الإدارة** (مفتاح/نص في `settings`).
4. حدّث الأدلة الثلاثة مع كل تغيير يخص المستخدم/الإدارة:
   `/guide` (العضو) · `/guide/store` (المتجر) · `/admin/guide` (الإدارة).
5. لا تكتب أي نص واجهة ثابتاً — اجعله في «النصوص الظاهرة» بالإدارة إن كان يخص المستخدم.
6. لا تضع الأسرار (كلمات المرور، `AUTH_SECRET`، كلمة مفتاح أندرويد) في المستودع.
7. عند تعديل مكوّن مُستخدَم في أكثر من مكان (مثل `ShareButtons`)، لا تُغيّر واجهته
   (props) دون تحديث **كل** نقاط الاستخدام، ودون حراسة SSR لأي وصول لكائنات
   المتصفح (`navigator`, `window`) بـ `typeof x !== 'undefined'`. تشغيل
   `npx tsc --noEmit` قبل الدفع يكشف هذا النوع من الأخطاء فوراً — لا تتجاهله.

---

## 9) استكشاف الأعطال

| العرض | الحل |
|---|---|
| فشل الاتصال بقاعدة البيانات | تحقق من `DATABASE_URL` وأن حاوية `db`/المضيف يقبل الاتصال من شبكة docker |
| الصور لا تظهر | تحقق من `STORAGE_DIR` و`LEGACY_MEDIA_BASE`؛ الصور القديمة تُبروكس من الأصل تلقائياً |
| الدخول لا يثبت | على HTTP اجعل `COOKIE_SECURE=false`؛ على HTTPS اجعله `true` |
| عمود مفقود بعد تحديث | أضِفه في `src/data/schema-sync.ts` و`prisma/schema.prisma` ثم `npx prisma generate` |
| معاينة رابط قديمة في واتساب | واتساب يخزّن المعاينة مؤقتاً؛ تتحدث تلقائياً خلال ساعات |

---

## 10) السلع المعتمدة والأقسام — تنفيذ سبتمبر 2026 دون تفعيل حي

الإعلانات العادية وإعلانات المتاجر تبقى للتواصل والاتفاق والدفع خارج المنصة. الاستثناء
الجديد هو سلع تربح المعتمدة صراحة، وليس كل إعلان مصنّف أو كل منتج متجر. العميل
يشتري من تربح؛ المورد طرف داخلي تديره الإدارة. يحتفظ النظام بلقطات التوريد
والمستحقات التشغيلية، بينما تسديد المورد يتم خارج النظام؛ لا دفع آلي للمورد،
ولا خصم من المحفظة ضمن مسار طلبات السلع.

### إعدادات التشغيل والصلاحيات

- إدارة السلع: `/admin/commerce`؛ الكتالوج `/shop`. مفاتيح `commerce_enabled` و
  `commerce_payments_enabled` و`commerce_notifications_enabled` مستقلة ومعطلة افتراضياً.
  النصوص والعنوان والوصف ورسائل الحالات والشحن قابلة للتحرير هناك؛ الرسم الخادمي
  `commerce_shipping_fee_sar` يجب أن يكون صريحاً، بما فيه الصفر، ولا يُؤخذ من المتصفح.
- إعدادات التجارة المالية تُقرأ عبر `src/lib/commerce/settings.ts` وتفشل مغلقة عند
  تعذر القراءة. ظهور الكتالوج ليس دليلاً على جاهزية الدفع.
- تكامل البنك الحالي غير مسجّل للتشغيل؛ هذا التنفيذ لا يعني تفعيل دفع حي أو قبول
  البنك للمعرّف العام `merchantOrderId`. يلزم تحقق عقد المزود ومضيف رابط الدفع
  والتحقق الخادمي في بيئة معزولة، ثم بوابات الاختبار والنسخ الاحتياطي والرجوع
  وموافقة تشغيل مستقلة. لا تُنسخ بيانات أو إعدادات الاختبار إلى الإنتاج.
- حدود الطلب تتحقق من المستخدم والصلاحيات؛ خدمات النواة تستقبل هوية عضو موثوقة
  وليست معرّفاً يختاره المتصفح. صلاحيات الإدارة منفصلة لخدمتي `commerce` و`categories`.

### ثوابت المال والطلبات والإشعارات

- `src/lib/commerce/money.ts`: المبالغ هلالات صحيحة مع تحليل عشري دقيق، بلا تقريب
  نتيجة التحقق القديمة الخاصة بشحن المحفظة. لا تتغير عقود مزودي المحفظة.
- `orders.ts`: لقطة السعر والسلعة والعنوان والهاتف والشحن تُحفظ خادمياً، وحجز المخزون
  ذري. مفتاح العضو والطلب فريد؛ إعادة نفس المدخلات تعيد الطلب، والمحتوى المختلف يُرفض.
- محاولة دفع واحدة لكل طلب، تُحجز قبل الشبكة؛ الجلسة المحفوظة تُعاد عند التكرار.
  محاولة غير محسومة لا تُنشأ مجدداً تلقائياً. لا تحرر مخزون طلب له أي محاولة؛ إلغاء
  الطلب غير المبدوء فقط يعيد المخزون مرة واحدة. تعديل المخزون الإداري بفارق ذري، لا
  باستبداله بقيمة قديمة من النموذج.
- التسوية تتطلب دليلاً خادمياً موثقاً مطابقاً للمبلغ والعملة والمرجع ورقم طلب التاجر.
  عودة المتصفح ليست دليلاً. انتقال الطلب إلى مدفوع وسجل التدقيق ورسائل الصندوق الصادر
  لكل مستلم وقناة يتم في معاملة واحدة، ولا يتكرر الأثر المالي.
- `notifications.ts`: حجز إرسال واحد `pending → sending → sent|unknown`؛ لا إعادة
  آلية للحالة غير المحسومة ولا إعادة خصم بسبب فشل رسالة. التعطل أثناء الإرسال يستلزم
  مراجعة يدوية. الحمولة رقم الطلب والمبلغ والعملة ومرجع الدفع فقط، بلا أسرار أو بطاقة.
- `schema.ts` يصدّر DDL الإضافي، والمخطط موثق أيضاً في Prisma؛ بوابة الجاهزية تتحقق
  من الجداول والأعمدة والفهارس الفريدة المطلوبة. معرّفات مزودي الدفع حساسة لحالة الأحرف.

### الموردون والسجل التشغيلي للمدفوعات

النطاق المعتمد موثق في `docs/superpowers/plans/2026-09-19-suppliers.md`؛ إضافة
هذه الإعدادات والجداول لا تعني تفعيل اتصال بمورد أو دفع حي.

- إدارة الموردين `/admin/suppliers` بصلاحيات `suppliers` المستقلة. جدول
  `commerce_suppliers` يحفظ الملف وبيانات التواصل والتسجيل والضرائب وشروط التسوية
  والملاحظات. `active` و`api_enabled` مفتاحان مستقلان، كلاهما معطل افتراضياً؛
  إيقاف المورد يمنع الطلبات الجديدة المرتبطة به، ولا يعيد كتابة اللقطات السابقة.
- `api_base_url` و`api_credential_ref` إعدادان اختياريان فقط. العنوان HTTPS بلا
  بيانات دخول أو query أو fragment أو عنوان IP أو مضيف محلي؛ المرجع اسم متغير
  بيئة مخصص بالنمط `TRBHH_SUPPLIER_[A-Z][A-Z0-9_]{0,79}`، وليس قيمة السر.
  الحفظ لا يجري طلب شبكة ولا يقرأ قيمة المتغير أو يعرضها أو يسجلها. لا يوجد
  موائم مورد مسجل؛ طلب تفعيل API مرفوض حتى اكتمال تكامل مُراجع. أي موائم مستقبلي
  يحتاج تحقق DNS/IP وقائمة وجهات مسموحة ومنع إعادة التوجيه قبل الاتصال؛ التحقق
  النصي من العنوان وحده ليس تصريحاً باستخدامه على الشبكة.
- `commerce_product_suppliers` يربط السلعة المعتمدة بـ `supplier_id` و
  `supplier_sku` و`unit_cost_minor` بعملة SAR. التكلفة هلالات صحيحة، مستقلة عن
  سعر البيع. غياب الربط يعني مخزوناً داخلياً غير مسند، ولا يُنشأ مورد افتراضي.
- عند إنشاء الطلب تُحفظ `commerce_order_suppliers`: لقطة المورد واسمه ورمز
  السلعة والكمية وتكلفة الوحدة وإجمالي التكلفة لكل طلب/سلعة. تعديل ملف المورد أو
  الربط أو التكلفة لا يغير الطلبات السابقة. هذه بيانات داخلية لا تدخل الإسقاط
  العام للطلب أو HTML أو حمولة مكونات العميل أو إشعارات العميل.
- التسوية الموثقة من البنك تنشئ `commerce_receipts` مرة واحدة لكل طلب، مع
  فرادة المزود/المرجع، والمبلغ والعملة والمرجع الموثق ووقت التسجيل. وتنشئ
  `commerce_supplier_accruals` مرة واحدة لكل لقطة طلب/سلعة بحالة
  `offline_pending`. انتقال الدفع والإيصال والمستحقات ضمن المعاملة نفسها؛
  الفشل يرجع الجميع، وتكرار التسوية لا يكرر الأثر. لا إيصال ولا استحقاق قبل
  تحقق دليل البنك، ولا مسار يدوي لاعتماد الدفع.
- السجل تشغيلي مساعد وليس نظام محاسبة نظامياً متكاملاً أو فاتورة ضريبية.
  `offline_pending` يعني انتظار التسوية الخارجية ولا يثبت الدفع للمورد.
  لا توجد تحويلات تلقائية للموردين، ولا تغيير لمسار المحفظة. بيانات المورد
  والتكاليف ومراجع الأسرار خاصة بالإدارة؛ واجهات العضو والمتجر توضّح فقط أن
  البائع هو تربح وأن تأكيد الدفع يتطلب تحقق الخادم من البنك.

### الأقسام الجديدة

التنفيذ المعتمد الجديد يحل محل تعليمات الأقسام القديمة في § 4، ولا يعتمد `cats_on`.
الإدارة `/admin/categories`، والمفتاح `categories_v2_enabled` معطل افتراضياً؛ النصوص
في `categories_v2_label_*`. التعريفات في `ad_category_definitions` والقيم في
`ad_category_values` مرتبطة بالإعلان الحقيقي. الحفظ يتحقق من تبعية القسم الفرعي
وإصدار التعريف ويكتب الإعلان والقيم في معاملة واحدة؛ النماذج القديمة تُعاد قراءتها
عند تغير التعريف. الحقول الاختيارية الفارغة مخفية، والوظائف لا تستخدم سعر البيع أو
حالة السلعة. التصنيف لا يمنح اعتماد بيع باسم تربح ولا يعيد تصنيف القديم آلياً.

الأدلة العامة تعرض الإضافات عند تفعيل الميزة فقط وتستخدم عنوان التجارة ووصفها
المضبوطين في الإعدادات؛ دليل الإدارة يبقى مرجعاً للتهيئة حتى مع تعطيل الميزات.

*آخر تحديث لهذا الدليل: يُحدَّث مع كل نسخة احتياطية.*
