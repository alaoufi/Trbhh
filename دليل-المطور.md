# دليل المطوّر — منصة تربح (Trbhh)

منصة إعلانات مبوّبة وأعمال تجارية B2B، عربية RTL، مبنية بـ **Next.js 15 (App Router)**
و **TypeScript** و **Prisma** على **MySQL 8**، مع **Redis** للكاش، ومنشورة على
**Hostinger VPS** عبر **Docker Compose**. الموقع المباشر: https://trbhh.com

> المنصة **ليست وسيطاً مالياً** — دورها عرض الإعلانات وربط الأطراف فقط، وكل تعامل أو
> دفع يتم خارج المنصة مباشرة بين الطرفين.

---

## 1) محتويات هذه النسخة الاحتياطية

| المجلد/الملف | المحتوى |
|---|---|
| `src/` | كل كود التطبيق (صفحات، مكوّنات، مكتبات، Server Actions) |
| `prisma/schema.prisma` | مخطط قاعدة البيانات (مستخرج من القاعدة الأصلية) |
| `database/trbhh.sql.gz` | نسخة قاعدة البيانات (mysqldump مضغوط) |
| `scripts/` | سكربتات النشر والنسخ الاحتياطي والاستيراد و SSL |
| `docker-compose.yml` · `Dockerfile` | حاويات التشغيل |
| `.env.example` | نموذج متغيّرات البيئة (انسخه إلى `.env` واملأ القيم) |
| `apps/android-twa/` | غلاف تطبيق أندرويد (TWA) |
| `دليل-المطور.md` | هذا الملف |

> **مهم:** ملف `.env` الحقيقي (كلمات المرور والأسرار) **غير مضمّن** في هذه النسخة
> لأسباب أمنية. أنشئه من `.env.example`. وكلمة مرور توقيع تطبيق أندرويد **ليست في
> المستودع** ويحتفظ بها المالك.

---

## 2) التقنيات

| الطبقة | التقنية |
|---|---|
| الواجهة | Next.js 15 (App Router) · React 19 · TypeScript · TailwindCSS |
| الخادم | Server Components + Server Actions |
| قاعدة البيانات | MySQL 8 · Prisma ORM |
| الكاش/الجلسات | Redis (ioredis) · جلسات JWT موقّعة (jose) |
| الصور | next/image + مسار وسائط موحّد `/media` |
| النشر | Docker · Nginx · Let's Encrypt |
| حزمة الحزم | pnpm |

---

## 3) التشغيل محلياً للتطوير

```bash
# 1) المتطلبات: Node.js 20+، pnpm، Docker (اختياري لقاعدة بيانات محلية)
pnpm install

# 2) البيئة
cp .env.example .env
#   عدّل DATABASE_URL و AUTH_SECRET (ولّده: openssl rand -hex 32)

# 3) قاعدة بيانات محلية للتجربة (اختياري)
docker compose --profile localdb up -d db redis
bash scripts/import-db.sh database/trbhh.sql.gz
npx prisma generate

# 4) التشغيل
pnpm dev            # http://localhost:3000
```

### بوابات الجودة (شغّلها قبل أي دفع)
```bash
npx tsc --noEmit    # فحص الأنواع
pnpm lint           # ESLint
pnpm test           # Vitest (35 اختبار)
pnpm build          # بناء الإنتاج
```

---

## 4) بنية المشروع

```
src/
  app/                 صفحات App Router + Server Actions (actions.ts بكل مجلد)
    (public)           الرئيسية، الأقسام، البحث، تفاصيل الإعلان، المتاجر…
    account/           لوحة العضو: الإعلانات، المحفظة، الملف، الشركة
    admin/             لوحة الإدارة (محمية بالصلاحيات) + admin/guide دليلها
    ads/ classified/   الإعلانات والإعلانات المبوّبة
    api/  media/       مسارات API وخدمة الوسائط
  components/          مكوّنات واجهة قابلة لإعادة الاستخدام
  lib/                 منطق الخادم: prisma, auth, roles, wallet, settings, data…
  data/schema-sync.ts  ضبط أعمدة/جداول ناقصة تلقائياً عند الإقلاع
prisma/schema.prisma   مخطط Prisma
scripts/               نشر ونسخ احتياطي واستيراد و SSL
```

### مفاهيم أساسية
- **الصلاحيات:** `src/lib/roles.ts` — `requireAction(service, action)` و`requireAnyAdmin()`.
- **الإعدادات والنصوص:** `src/lib/settings.ts` — كل نص/مفتاح قابل للتعديل من لوحة الإدارة
  (النصوص الظاهرة، الإعدادات) عبر جدول `site_settings`. لا تكتب نصوصاً ثابتة في الكود.
- **المحفظة:** `src/lib/wallet.ts` — `charge/adjustBalance` وأسباب الحركات `TxnReason`.
- **مخطط متسامح:** `src/data/schema-sync.ts` يضيف الأعمدة الناقصة تلقائياً — عند إضافة
  عمود جديد، أضِفه هنا **و** في `prisma/schema.prisma`.
- **توافق كلمات المرور القديمة:** دخول الأعضاء الحاليين يعمل بكلمات مرورهم القديمة
  (bcrypt `$2y$`).

---

## 5) النشر على الخادم (Hostinger VPS)

المستودع على الخادم في `/root/trbhh`، وخدمة التطبيق في docker compose اسمها `app`.
فرع التطوير الحالي: `claude/hostinger-vps-project-amw8vb`.

### النشر التلقائي (الطريقة الأساسية)

كل دفع (`git push`) لفرع `claude/hostinger-vps-project-amw8vb` يُشغّل تلقائياً سير عمل
GitHub Actions **`.github/workflows/deploy.yml`**: يتّصل بالخادم عبر SSH، يسحب أحدث الكود،
ويعيد بناء وتشغيل خدمة `app` في docker compose — **بلا أي تدخل يدوي**. يمكن تشغيله يدوياً
أيضاً من تبويب **Actions** في المستودع (`Run workflow`).

**الأسرار المطلوبة** (Settings → Secrets and variables → Actions في المستودع):
| السرّ | القيمة |
|---|---|
| `VPS_HOST` | عنوان IP الخادم |
| `VPS_USER` | `root` |
| `VPS_PORT` | اختياري — افتراضياً 22 |
| `VPS_SSH_KEY` | المفتاح الخاص (نص خام كامل الأسطر، من `BEGIN` إلى `END`) — مفتاحه العام مُضاف مسبقاً في `~/.ssh/authorized_keys` على الخادم بتعليق `github-actions-deploy@trbhh` |

السجلات: تبويب **Actions** في المستودع، أو من الخادم `docker compose logs -f app`.

### النشر اليدوي (بديل احتياطي)

يعمل دائماً بغضّ النظر عن حالة النشر التلقائي:

```bash
cd /root/trbhh
git pull origin <اسم-الفرع>
docker compose up -d --build app
docker compose ps
```

- إعادة التشغيل: `docker compose restart app`

### متغيّرات البيئة المهمة (`.env`)
- `DATABASE_URL` — رابط قاعدة البيانات (رمّز الأحرف الخاصة في كلمة المرور: `@`→`%40`، `#`→`%23`).
- `AUTH_SECRET` — سر توقيع الجلسات (`openssl rand -hex 32`).
- `STORAGE_DIR` — مجلد الوسائط المرفوعة (مربوط كـ volume).
- `COOKIE_SECURE=true` خلف HTTPS فقط.
- التفاصيل الكاملة في `.env.example`.

---

## 6) قاعدة البيانات — نسخ واستعادة

### نسخة احتياطية للقاعدة الحيّة (على الخادم)
```bash
cd /root/trbhh
bash scripts/backup.sh          # ينتج backups/trbhh-<تاريخ>.sql.gz (يحفظ آخر 14)
```

### نسخة احتياطية كاملة (كود + قاعدة حيّة + وسائط + هذا الدليل) في ملف واحد
```bash
cd /root/trbhh
bash scripts/full-backup.sh     # ينتج تربح-<تاريخ>-<وقت>.zip
```

### استعادة قاعدة بيانات من ملف
```bash
bash scripts/import-db.sh <ملف.sql.gz>
```

> ملاحظة: `database/trbhh.sql.gz` المضمّن في النسخة هو لقطة بتاريخ إنشائه، وليس
> بالضرورة أحدث بيانات الإنتاج. للحصول على أحدث البيانات استخدم `scripts/backup.sh`
> أو `scripts/full-backup.sh` على الخادم.

---

## 7) تطبيق أندرويد (TWA)

- المجلد: `apps/android-twa/` — الحزمة `com.trbhh.app`، المُوقّع (alias) `trbhh`.
- **كلمة مرور المفتاح (keystore) ليست في المستودع** — يحتفظ بها المالك، وتُدخل يدوياً عند البناء.
- الإصدار يُرفع في ملفات إعداد Gradle (versionName/versionCode).

---

## 8) قواعد ثابتة للمشروع (اتبعها في أي تعديل)

1. شغّل بوابات الجودة (tsc/lint/test/build) **قبل أي دفع**.
2. رسائل الالتزام (commit) بالعربية.
3. أي ميزة جديدة يجب أن تكون **قابلة للتحكم من لوحة الإدارة** (مفتاح/نص في `settings`).
4. حدّث الأدلة الثلاثة مع كل تغيير يخص المستخدم/الإدارة:
   `/guide` (العضو) · `/guide/store` (المتجر) · `/admin/guide` (الإدارة).
5. لا تكتب أي نص واجهة ثابتاً — اجعله في «النصوص الظاهرة» بالإدارة إن كان يخص المستخدم.
6. لا تضع الأسرار (كلمات المرور، `AUTH_SECRET`، كلمة مفتاح أندرويد) في المستودع.

---

## 9) استكشاف الأعطال

| العرض | الحل |
|---|---|
| فشل الاتصال بقاعدة البيانات | تحقق من `DATABASE_URL` وأن حاوية `db`/المضيف يقبل الاتصال من شبكة docker |
| الصور لا تظهر | تحقق من `STORAGE_DIR` و`LEGACY_MEDIA_BASE`؛ الصور القديمة تُبروكس من الأصل تلقائياً |
| الدخول لا يثبت | على HTTP اجعل `COOKIE_SECURE=false`؛ على HTTPS اجعله `true` |
| عمود مفقود بعد تحديث | أضِفه في `src/data/schema-sync.ts` و`prisma/schema.prisma` ثم `npx prisma generate` |
| معاينة رابط قديمة في واتساب | واتساب يخزّن المعاينة مؤقتاً؛ تتحدث تلقائياً خلال ساعات |

---

*آخر تحديث لهذا الدليل: يُحدَّث مع كل نسخة احتياطية.*
